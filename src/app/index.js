const db = require('./db');
const swapiFunctions = require('./swapiFunctions');
const planet = require('./Planet');
const people = require('./People');

module.exports = {
    db,
    swapiFunctions,
    planet,
    people
}